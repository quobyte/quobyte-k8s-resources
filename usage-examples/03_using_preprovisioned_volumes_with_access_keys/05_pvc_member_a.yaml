kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: shared-volume-claim-a
  annotations: {
    user-secret: member-a-secret, 
  }
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: quobyte-byos 
  volumeName: pre-provisioned-volume-a 
